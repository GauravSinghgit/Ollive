# Core UI
gradio>=4.44.0
python-dotenv>=1.0.0

# Both models served via Groq API
groq>=0.11.0

# OSS model (Qwen2.5 via transformers)
transformers>=4.45.0
torch>=2.1.0
accelerate>=0.26.0
sentencepiece>=0.2.0
tokenizers>=0.19.0

# Evaluation & reporting
matplotlib>=3.8.0
numpy>=1.26.0

# Optional: quantization for faster CPU inference
# bitsandbytes>=0.41.0
